# AutoQuill 更新日志

版本号以 core/version.py 为唯一事实来源（发布 tag 为 v<VERSION>）。

## v4.9.3（开发中）

### 新功能：自动化模块（24 小时时间轴调度 · 无人化运营，M1 骨架+首个任务）
- **定位**：把「每天打开软件挨个点按钮」变成「给时间轴派活，到点自动执行，想干预时才进去看」。
  规划全文见 `docs/AUTOMATION-PLAN.md`
- **时间轴与排班**（`automation/planner.py`）：按「任务类型 + 每日配额 + 运行时段 +
  最小间隔（≥1 小时，可随机上浮）+ 抖动」展开当天作业；**不固定时间点**（当日完成即可）；
  随机种子 = (日期, 计划指纹) → 重启后时间轴稳定，不会每次刷新都变
- **执行与续做**（`automation/scheduler.py` + `executor.py`）：tick 只做时间比较（极轻，
  浏览器只在作业真正执行时拉起）；同一时刻只跑一个作业；**手动操作优先**（浏览器被占用则
  顺延、不算失败）；**随时可停**，再次开始时按台账统计「今天已发布/已写多少」只补差额；
  失败当日补位（最多补 2 次），连续失败 3 次自动熔断该类任务并通知；登录失效/通道未配置
  → 暂停全部自动化并提示（不硬闯）
- **首批任务**：**全链路撰写**已可用（复用经典/纯净完整链路，一次作业 = 一篇）；
  发布草稿（草稿箱按「从旧到新」发布）、打卡挑战、感谢赞同/喜欢、回复评论只登记契约，
  UI 显示「待接入」，按里程碑逐个接入
- **可视化**（`webui/static/automation.js` + style.css）：24 小时刻度 + 每类任务一条泳道 +
  状态配色（待执行/执行中/已完成/失败/已跳过/需要人工）+ 「现在」指针 + 运行时段底纹；
  今日进度卡（配额 / 下一次执行 / 运行时段）；点作业块看详情（计划·实际·产出·说明）；
  左侧控制台：开始 / 暂停 / 停止 / 立即执行 + 时段与间隔设置 + 任务配额（改动即存）
- **API**：`GET /api/automation`（状态）、`/api/automation/types`（任务目录）、
  `/api/automation/history`（台账）；`POST /api/automation/plan|start|stop|pause|resume|run-now`
- **数据**：`data/state/automation/{plan.json, day_YYYY-MM-DD.json, ledger.jsonl}`
  （本机运行数据，已加入 .gitignore）
- **测试**：新增 `tests/test_automation_core.py` 23 例（排班配额/时段/间隔、续做、错过补做、
  去碰撞、幂等、浏览器占用顺延、需要人工暂停、熔断、手动立即执行 + API 边界）；
  `tools/auto_test.py` 新增自动化前端四项检查（五大模式/泳道/刻度图例/控制与配置/进度卡）；
  全量 `tests/run_all.py` **578 例 0 失败**；`tools/auto_test.py` 全流程 **27/27 通过**
- 新增 `tools/archive/probes/shot_automation.py`：独立数据目录 + 造当天排班 → 截图时间轴
  （改样式后一眼验收，产物 data/cleanup/auto_timeline.png）

### 待办（自动化后续里程碑）
- **M2 发布草稿**：真机探针确认「草稿编辑页 → 发布 → 确认弹窗 → 校验已发布」的 DOM，
  接入后按「从旧到新」发布并注入任务（用户第一优先项）
- **M4 托盘常驻**：关窗最小化到托盘（pywebview WinForms NotifyIcon 先做 spike）、
  可选开机自启（默认关闭）
- **M3 互动类**：打卡挑战 / 感谢赞同喜欢 / 评论回复（按 M0 探针结论接入；评论回复默认
  「生成后待批准」）

## v4.9.2（2026-09-19）

### 改进（2026-09-19：看板拆成「统计 / 明细」双视图 + 统计页重排）
- **视图切换**：看板标题栏新增「统计 / 明细」分段控件（默认统计，选择记 localStorage）——
  统计 = 6 张 KPI + 数据摘要条 + 6 个图表卡；明细 = 筛选 chips + 表格工具条 + 表格。
  两个视图各自完整，不再把 KPI、图表 tab、表格堆在同一列里互相打架
- **统计页重排**：KPI 六卡（已发布 / 阅读合计 / 赞同合计 / 评论合计 / 赞同率 / 篇均互动）
  + 摘要条（统计口径 / 数据窗口 / 快照总量与本次纳入量）；图表改为 12 栅格卡片：
  发布节奏(8) + 互动漏斗(4)、赞同分布(6) + 题材分布(6)、Top20(7) + 阅读·赞同散点(5)；
  **去掉原来的图表 tab**（一次看全，不必来回点）；窄屏自动收敛单列、KPI 降为 3/2 列
- **修「图看不到」**（用户实测反馈）：应用窗口默认 1280×820，统计内容比看板卡高，而看板卡
  是固定高度的 flex 列且没有滚动 → 下面的图表被直接裁掉。现在 `#dashViewStats` 自带
  纵向滚动（`overflow-y:auto`），并把中屏排版收紧：KPI 一行 6 张（≤1180 才收 3 列）、
  图表高度 190/250（≤1440 降到 176/224）、卡片内边距缩小——1280×820 下内容高 836px，
  只需一次轻微滚动即可看全，不再有「超出界面框看不到」的部分
- **图表细节**：互动漏斗标签改到形状内部（原来「阅读 1013」会被卡片右边缘截断）+ 分段提亮；
  题材分布改用右侧竖排图例、扇区不再挂外标签（窄卡片下不再拥挤/被裁）；
  Top20 标签栏加宽 + 逆序（高赞在上）+ 轴指示 tooltip；赞同分布把最高峰
  高亮；漏斗左右留白与间隙调整；散点/分布统一走基础主题色
- **明细页**：只留筛选 chips + 表格（粘性表头、数字右对齐 + tabular-nums、行悬浮高亮照旧）；
  标题栏「显示 / 共 N 条」「快照日期」两颗 pill 只在明细视图显示（统计视图由摘要条承担）
- 新增 `tools/archive/probes/shot_dashboard.py`：起临时服务 + 造一批接近真实的样例数据 →
  截图统计/明细两个视图，并内置「末图可达性」自检（1280/1100/960 三种宽度实测 PASS，
  滚动容器分别为 #dashViewStats 与 window）；产物 data/cleanup/dash_*.png
- 测试：新增 tests/test_dashboard_views.py（7 例：视图开关与容器、六图六卡、旧 tab 逻辑清除、
  localStorage 记忆与 charts resize、KPI 六项、栅格 CSS 与响应式、auto_test 同步）；
  tools/auto_test.py 看板断言按新 DOM 重写，并修正草稿预览入口（点整行是打开知乎编辑页，
  预览要点行内「详情」按钮——原断言与实际 UI 不符，属历史遗留）
- 验证：tests/run_all.py **561 例 0 失败**；tools/auto_test.py 全流程 **23/23 通过**（含 Edge 真实渲染）

### 修复（2026-09-19：看板/草稿箱「刷新失败」与「删了本地不同步」）
- **问题①现象**：看板与草稿箱刷新都只报「刷新失败 / 未取到有效数据」，用户不知道
  该怎么办。**真因**：知乎服务端把会话登出了——本机 cookie 里的 z_c0 还在
  （`is_logged_in()` 只看 cookie → 仍报「已登录」），但任何页面都会被 302 到
  `/signin`，抓取自然 0 条。用真实 profile 与保存的登录态文件分别实测：可见/无头
  浏览器都停在登录页 → 是登录态失效，不是页面改版、也不是无头被识别
- **修复①（识别 + 可执行提示）**：新增 `page_needs_login()` / `ZhihuLoginRequired`
  （browser_adapter）。看板与草稿箱的抓取、以及两条删除链路，导航后一旦发现停在
  登录页就立即中止并抛专用异常；API 层捕获取而代之的模糊文案，前端显示
  「知乎登录已失效：请点右上角「设置」重新登录知乎，再回来刷新」（15 秒不自动消失），
  同时把失效标记经 `/api/setup/status` 带给首启引导（知乎步骤显示 ⚠ 并给出重新登录按钮），
  不再假装「已登录」
- **问题②现象**：在知乎上删掉已发布回答/草稿后，本地快照不更新，用户必须整页重抓。
  **修复②（删除即同步本地）**：新增共用助手 `_snapshot.prune_rows()`；
  `published.prune_aids()` 改走它，草稿箱补上 `drafts.prune_qids()`。
  看板/草稿箱的删除任务在知乎删除成功后**自动剔除本地快照对应行**，并把
  `removed` 条数回给前端（提示「已删除 N/M 条，本地看板同步移除 N 条」）；
  前端删除完成后本来就会重载列表，因此删完立刻消失，无需再刷新整页
- **诊断脚本**：新增 `tools/archive/probes/probe_creator_dom.py`（创作中心 DOM 结构
  与候选类名统计）与 `probe_state_login.py`（用保存的登录态直连验证是否被跳登录页），
  下次再遇「抓取 0 条」可一条命令定性是登录态还是页面改版
- **问题③现象（用户提问）**：装好之后在界面里找不到登录知乎的入口——登录按钮只
  存在于首启引导的第 3 步，而「有登录态文件」就被判为已完成，连引导里的按钮都被
  隐藏；一旦会话被服务端登出，用户没有任何地方可以重新登录。
  **修复③（常驻登录入口 + 真实检查）**：设置弹窗新增「知乎账号」区块——显示当前状态
  （未登录 / 已保存登录态 / ⚠ 登录态已失效）、「检查登录状态」与「重新登录知乎」两个
  按钮。检查走新增端点 `POST /api/setup/zhihu-check`：真实打开知乎首页看是否被重定向
  到登录页（不再只看 cookie），并把结果同步到失效标记；重新登录复用既有可见 Edge 引导，
  完成后自动实测确认。新增 `browser_adapter.verify_zhihu_login()` 供两处共用。
  首启引导的知乎步骤在「失效」状态下也会重新显示登录按钮（此前会被判「已完成」而隐藏）
- **测试**：新增 tests/test_local_sync_and_login.py（12 例：快照剔除的命中/空值/坏文件/
  取最新文件、两个业务包装、登录页识别与异常安全、抓取与删除链路的守卫、删除端点同步本地）
  与 tests/test_zhihu_login_entry.py（8 例：真实检查的登录/登出/异常三态、检查端点的
  失效标记置位与清除、任务占用时不开浏览器、设置弹窗入口接线）

### 测试
- 全量 tests/run_all.py：**561 例，0 失败 0 错误**（浏览器依赖用例自动跳过）
- tools/auto_test.py：全流程 **23/23 通过**（含 Edge 真实渲染：四大模式/设置弹窗/看板
  统计与明细视图/分页/筛选 chips/草稿箱预览与删除流/无 console 错误/服务端日志无 ERROR）
- 用户真实环境验证（dev 版日志 2026-09-19 15:27–15:40）：重新登录知乎后看板抓取 405 条、
  草稿箱 15 个；删除 2 个草稿与 7 条已发布内容后，本地快照分别自动剔除 2 / 7 条

## v4.9.1（2026-09-19）

### 修复（2026-09-19：经典模式篇篇「我」字开头，开篇同质化）
- **现象**：经典模式最近 22 篇里 20 篇以「我」开头（最近 20 篇 100%、今天 16/16），
  且都是同构的「我 + 强动作/物件/数字」（我撬开丈夫的抽屉 / 我把离婚协议放在茶几上 /
  我数了数…）
- **根因（不是模型能力，是守则叠加把开头挤成唯一解）**：① 写作守则第 6 条「确立唯一
  叙述声口：第一人称就要有『我的声口』」+ 作者签名（闲得无聊的仙女）里的「第一人称
  内心OS」；② 引言一票否决（正文第一行必须是正文）；③ 禁止空镜开场 +「第一行直接进入
  动作或对话」→ 最省力的合规解只剩「我+动作」。实测排除项：参考素材（高赞回答）本身
  只有 23% 以「我」开头，不是它带偏的；纯净模式（不注入这些守则）只有 33%，同批数据
  里对比明显
- **修复一（守则）**：`story_prompt.OPENING_VARIETY_RULE` 新增「开篇起手式多样化」硬条款，
  写明作者签名的「第一人称」是声口偏好而非「每篇我起手」，并给出 5 种起手式菜单（对话 /
  他人 / 物件 / 时间·数字断语 / 反差断语），要求引言第一句不默认落回「我+动作」；每种
  起手式都要求在首段出现人/对话，避免被判「空镜开场」扣分；不放宽任何格式门槛
- **修复二（起手方式以参考文章为准，不搞固定循环）**：`analyze_reference_opening()` 本地
  识别「本题最受认可那篇参考文章」的起手方式（对话/他人/物件/时间·数字/判断断语/
  第一人称/场景；纯启发式、零 LLM 调用），`build_story_prompt` 据此注入「本篇开篇起手
  方式：学它的手法、换掉内容与措辞」；参考本身就是「我」起手时额外要求换句式与切入口
  （别把同构学回来）。**只有没有参考文章时才用 5 种起手式轮换兜底**
  （`next_opening_style()`，多线程安全）——轮换是兜底而非固定指派。选择写进日志 mode_str
  （如 `采样模式… · 起手式:参考起手:物件起手`）
- **修复三（模仿的「度」）**：新增 `core/originality.opening_copy_signals()`——故事引言 vs
  参考回答开头的最长连续重合达到阈值（`OPENING_COPY_MIN_RUN`，默认 12 字）即判抄；
  `generate_story_with_retry` 命中后带反馈重写（只允许学起手手法，句子/人物/道具/情节
  必须全部换新），三次仍命中则整轮弃稿（宁可不发也不冒险）
- **开关**：`config/story.py` 的 `OPENING_MIRROR_REFERENCE`（参考优先，默认 True）、
  `OPENING_VARIETY`（无参考时轮换兜底，默认 True）、`OPENING_COPY_MIN_RUN`（抄袭红线，默认 12）
- **实测参考素材的起手分布**（collected_stories 35 篇，本地识别）：判断/反差断语 51%、
  第一人称 23%、物件 23%、时间·数字 3% —— 按题模仿参考天然就有多样性，不需要人为循环
- 新增/更新测试：tests/test_opening_variety.py（18 例：轮换覆盖与回环、多线程不重不漏、
  参考起手识别、注入策略（参考优先 / 轮换兜底 / 显式覆盖 / 全关）、抄袭红线阈值与片段）；
  tests/test_generate_retry.py 新增 2 例（命中红线重写 / 三次都命中弃稿）

### 首启引导与文档（同日修订）
- 首启引导文案不再写死 DeepSeek：Web 卡片说明与登录按钮改为「网页版大模型
  （DeepSeek / 豆包）」（按钮文字本由前端按当前驱动动态覆盖，这里修默认值）
- README：标题与安装步骤版本号同步；版本历史补 v4.9.1 / v4.9.0 / v4.8.1；FAQ 模式名更新；
  Web 通道成本行补豆包
- docs/DEVELOPER.md：架构图补 doubao.py 与 base 的 markdown 逐块重建；探测脚本路径修正
  （三个脚本已归档到 tools/archive/probes/）；8.3「新增 Web LLM 驱动」补齐两条硬约束
- docs/QA-PLAYBOOK.md：发版流程按 2026-09-19 实跑校准（先提交 → build --skip-browser →
  提交 iss 版本号 → tag/push → gh release create → 回下载比对 sha256 → 盯 CI）
- docs/PROJECT_MEMORY.md / AGENT-OPERATING-NOTES.md：发布记录、网页端提取坑、网络实测说明同步

### 测试
- 全量 tests/run_all.py：534 例，0 失败 0 错误（浏览器依赖用例自动跳过）
- tools/auto_test.py --quick：后端单测 + Python 文件语法 + app.js 语法全通过
- 用户真实运行验证：经典模式开篇起手方式已按参考文章走（本篇 log 可见「起手式:参考起手:…」）


## v4.9.0（2026-09-19）

### 适配 2026-09 DeepSeek 官网改版（网页端重新具备主力资格）
- **取消模式与开关切换**：官网取消「快速 / 专家 / 识图」三大模式，只留
  「深度思考 / 智能搜索」两个开关。`setup()` 改为空操作——登录后用账号默认状态，
  不再点任何模式 tab 或开关（旧 `radiogroup` 已不存在，留着只会打出误导性日志）
- **多轮读取锚定最新回复（修线上误判）**：改版后消息列表是虚拟列表（每条消息
  一个 `div.ds-message`，旧回复仍挂在 DOM 里），旧实现用 `querySelector` 取「第一个
  正文容器」→ 多轮会话读到上一轮回复（2026-09-09 真实日志：生成 prompt 发出
  11s 后读回上一步筛选的 191 字，误判「故事过短」重试）。现在发送前给最后一条
  消息打锚点，之后只读锚点之后的新消息；一条都没读到就 loud-fail，绝不拿旧回复顶包
- **会话删除打通（改版后真实链路）**：`POST /api/v0/chat_session/delete`（body
  `{"chat_session_ids": ["<uuid>"]}`，Bearer 取页面 `localStorage.userToken`；旧 `/api/v0/chat/*`
  端点已随改版下线、且不带 Bearer 会 401）+ DOM 兜底「侧栏项悬停 → ⋯ → 菜单
  删除 → 弹窗『删除该对话』」。成功还要过业务码（code/biz_code = 0）
- **前端/接口清理**：删除 `/api/web-preset` 端点、`WEB_PRESET` 配置项、设置面板
  「网页预设」行与 `set_web_mode_preset`；启动横幅不再显示模式名
- 真机 E2E（2026-09-12，无头）：两轮生成各自读回新回复（锚点生效）+ 会话删除成功

### 网页版会话管理（防风控）
- **单条完整链路只开一个聊天会话**：串行 Web 生成（经典/纯净/环节测试·生成）全程
  复用同一个网页会话——重试带反馈追问、评分/筛选/原创审核等连续请求走同会话续聊，
  不再反复新开会话；仅当原会话损坏（找不到输入框/发送失败/页面改版/生成超时）才
  自动新开会话
- **故事生成完成后自动删除该会话**：任务结束在网页端把本次会话删掉（DeepSeek 走
  站点内部删除 API + DOM 兜底），并行批量在 teardown 时逐个删除；只删本程序自己
  创建/使用过的会话，绝不误删用户已有会话；删除失败仅记日志不阻断任务

### 历史会话清理工具（30 天前写故事残留，一次性运维）
- **新增 `tools/ds_history_cleanup.py`**：自动删除上线前堆积的历史会话。三个子命令：
  `scan`（只读统计报告，不删任何东西）/ `delete`（默认 dry-run，`--yes` 才真删）/
  `smoke`（自建空会话→删除，自检删除链路，不碰历史数据）
- **两道判定闸**：① 最后活动（`updated_at`）早于 `--days` 天（默认 30）；
  ② 拉取会话消息匹配 AutoQuill 提示词指纹（story_prompt / zhihu_story.prompts 的签名
  短语，跨版本收录，权重 ≥2 即确认）。标题关键词只用于决定「值不值得拉内容」，
  本身不作为删除依据；默认跳过置顶会话
- **删除前逐条备份正文**（`data/cleanup/backup_<ts>/`，备份失败的条目直接不删），
  分批提交 + 连续失败熔断 + 删后重新枚举列表复核，全过程日志落
  `data/cleanup/delete_<ts>.json`
- 真机校准（2026-09-15，只读）：列表 `GET /api/v0/chat_session/fetch_page`
  （游标 `lte_cursor.updated_at`，每页 100）、内容
  `GET /api/v0/chat/history_messages?chat_session_id=…`。**坑**：不带站点
  `x-client-*` 客户端头时游标被忽略、每次只回第一页（实测 1243 条只读到 100 条）——
  客户端头改为进站时从页面自身请求捕获（版本升级自动跟随），缺省有兜底值
- **消息格式坑（首次真机扫描踩到）**：同站点新旧会话的消息结构不同——新会话正文在
  `message.content`，旧会话没有 content、正文在 `message.fragments[{type:REQUEST}].content`。
  只读 content 会把 2026-08 之前的所有会话读成「空内容」→ 指纹 0 命中 → 344 条真故事
  会话被判「无关」（漏删）。修复：`normalize_messages` 兼容两种结构
- 指纹表跨版本收录（V1.0.0 `请你扮演一位顶尖的故事创作大师` / V2.3.0
  `顶尖的知乎故事创作大师`+`开头铁律` / V3+ `知乎故事区`+`核心铁律`+生成守则与
  选题/评分/原创审核/配方/文风全链路）
- **真机全量扫描结果（2026-09-17，账号 1243 条会话）**：30 天前旧会话 1053 条 →
  165 条确认 AutoQuill 写故事链路（可删）、74 条模板式长提示词（疑似早期未入库版本生成，
  与 output/ 里同分钟的同款提示词产物对应 → 归入「写故事但非 AutoQuill」档，默认不删）、
  76 条用户手写写故事会话（盐选投稿咨询/拆书/300 字小故事，默认不删）、2 条弱指纹疑似、
  736 条无关。可读报告 `data/cleanup/report_20260917.md`
- **实际清理（2026-09-17 用户确认后执行）**：删除 314 条（165 确认 + 149 写故事会话；
  315 个目标里 1 条置顶的「语音转换」实为研究语音笔记，按 `跳过置顶` 保护性保留），
  失败 0 / 备份失败跳过 0；正文备份 `data/cleanup/backup_20260917_085832/`（315 文件 32MB），
  日志 `data/cleanup/delete_20260917_090054.json`。删后全量复核（739 条旧会话逐条拉内容）：
  确认命中 0、模板式 0，仅剩 1 条置顶手写会话 + 2 条「洗稿是什么意思」弱指纹
- 回归测试：`tests/test_ds_history_cleanup.py`（31 例：指纹/三态判定/响应解析/分页/
  消息格式兼容/delete 备份与熔断/删除档位）

### 适配更多网页端大模型（Web 驱动框架）
- 新增「网页版大模型」运行时切换（设置 → 生成通道 → 网页版大模型），随 webui_model.json
  持久化；首启引导/登录引导/登录态检测按当前驱动分发
- **豆包（Doubao）驱动已按真实登录页面 E2E 校准**（2026-09-07 实测 + 2026-09-08
  长文修复）：输入框 div[contenteditable=true]、Enter 发送、会话 ID 在 URL /chat/{id}、
  回复读取取 [class*=md-box-root] 里「最后一条用户消息之后的第一条助手消息」正文
  （innerText，保留段落）；模型固定「豆包快速」（用户约定）
- **会话删除（2026-09-12 两条链路都打通）**：
  · DeepSeek：POST /api/v0/chat_session/delete（Bearer 取页面 localStorage
    userToken）+ 侧栏「⋯ → 删除 → 删除该对话」DOM 兜底
  · 豆包：内部接口仍是 401，改走侧栏会话项「悬停 → ⋯ → 菜单『删除』→
    弹窗『确定删除对话?』」（操作按钮 hover 才显示，必须 Playwright 真
    hover；JS mouseover 无效）——此前「无头环境未命中菜单」的结论已修正
- **豆包「卡片式交付」兜底**：豆包对部分长写作请求会走独立写作/文档交付界面
  （对话里只留一句引言 + flow-product-card 卡片：标题 + 创建时间），正文不在
  对话 DOM 里。驱动读到短文本且「本次提问之后」出现该卡片容器时，在同一会话
  补问「直接输出全文正文，不要卡片/文档/任务交付界面」，最多 2 次；卡片渲染
  晚于对话文本稳定，补问前最多等 45s。实测有效：短故事 607 字、长暗恋文
  8022 字均完整取回
- 每个站点驱动独立封装（登录判定、登录引导、会话删除各站实现），新增站点只需在
  web_drivers 注册表与 config.WEB_DRIVERS 各加一条

### 修复（豆包切换实测暴露的问题）
- 登录态缓存按驱动名区分：DeepSeek 已登录 ≠ 豆包已登录（此前切豆包直接放行不弹登录）
- set_runtime_web_driver 持久化 web_driver 字段到 webui_model.json（此前重启丢失）
- 豆包不支持 fast/expert 预设：后端拒绝 + 前端隐藏预设行（按约定固定豆包快速）
  （2026-09 官网改版后 DeepSeek 也取消三大模式 → 预设机制整体退役，见下节）
- 豆包登录判定只认 passport 登录 cookie（sessionid 等），忽略匿名跟踪 cookie
  （此前未登录也会误判已登录、登录引导秒过不弹窗）

### 修复（2026-09-08：豆包模式读不到故事正文）
- **根因**：豆包对长写作请求走「写作/文档交付」——对话里只剩一句引言（86-91 字），
  正文在独立交付界面（不在对话 DOM 里）。驱动把引言当正文返回，故事被判「字数
  过短」连废 3 次（真实日志 2026-09-08 19:59：3 次重试都只读到 91 字，存盘 267
  字节废稿）
- **主修：prompt 明确要求内联输出**（story_prompt.INLINE_OUTPUT_RULE）——所有模式
  prompt 末尾追加「直接在对话里输出完整正文，不要使用卡片/文档/工作任务/附件等
  交付界面，不要任何前后缀说明，一次写完」。真实 E2E（8278 字 prompt）：修复前
  对话正文 91 字 + 交付卡片；修复后 6305 字全文内联、无卡片，格式校验 score=6 通过
- **驱动修正（取错正文）**：回复正文改为「最后一条用户消息之后的第一条助手消息」
  + innerText。原实现取「最后一个 md-box-root」：豆包回复后追加的追问建议条
  （如「需要我帮你微调每章字数吗？」）同样是 md-box，会被当成正文；发送后助手
  未开始输出时还会把用户自己刚发的 prompt 读成「已生成 10058 字」。textContent
  会把整篇故事压成一行（段落/章节行全丢，实测 6305 字挤成一行），innerText 保留换行
- **发送确认**：发送后以「用户消息条数增加」确认消息真的被接收。真实日志里上一轮
  写作任务仍在跑时豆包会忽略新输入，重试的 prompt 根本没发出去，随后读回的是上一条
  旧回复（3 次重试读到同一段 91 字）——现在未接收会重试一次发送按钮，仍失败标记
  会话损坏并抛错，不再把旧回复当成本次结果
- **站点 chrome 剥离**：豆包在正文首尾附带「我将贴合…风格/遵守…要求」写作计划句和
  「需要我帮你…吗？」追问建议条。clean_story_output 新增「写作计划」开场识别
  （计划动词 + ≥2 个写作元词，避免误删「我将永远记得那天」这类正常首句）；
  驱动剥离正文末尾的追问建议条

### 修复（2026-09-09：豆包全链路复核暴露的问题）
- **章节标题丢失导致格式判废（最关键）**：豆包把 markdown 渲染成 DOM
  （`## **N**` → `<h2>N</h2>`），只读 innerText 会丢掉 `## **` 语法 → 格式校验
  「章节 0 个」扣 4 分。真实全链路因此 3/10 判废（章节 0 个 -4 / 字数 3979<4000 -2 /
  量化 -1）。改为「逐块重建」：遍历消息的块级子元素，h1-h6 还原成 `## **N**`、
  其余块按段落拼接。同一篇真实输出重建后实测 **10/10 通过**（无需重试）
- **文本稳定 ≠ 生成结束**：豆包生成期间发送按钮 `#flow-end-msg-send` 是 disabled，
  正文却可能长时间不变（写作任务还在跑）。原判定按「文本稳定 2 轮」提前返回，
  紧接着的重试消息被豆包吞掉 → 发送确认失败 → 会话被判损坏、整轮报废
  （真实日志 21:43:21 判定完成 → 21:43:23 重试发送 → 21:44:13 失败退出，而豆包
  其实已把重试版故事写完，只是没被读取）。现在 `wait_complete` 判定完成前要求
  「UI 空闲（发送按钮可用）」，`send()` 发送前先等 UI 空闲（最长 240s）
- **发送确认改双判据**：用户消息条数增加 **或** 输入框从有内容变空（长对话里消息
  可能被虚拟化/延迟渲染，只数消息会被骗过）
- **发送按钮兜底选择器修正**：`[class*='send']` 会命中用户消息气泡、隐藏包装层等
  4 个元素 → Playwright strict mode violation（真实日志）。改用精确 id
  `#flow-end-msg-send` + `.first`

### 精简与进度条（2026-09-09 二次整理）
- **豆包模式终于有进度条**：心跳文案由「豆包生成中…」改为「故事生成中… 已生成
  N 字」——旧文案不匹配 `webui/log_capture._PROGRESS_RE`，前端进度条压根不出现
  （这就是豆包模式看不到进度条的原因）。现在与 DeepSeek 完全同源，前端显示
  「故事生成中… 已生成 N 字（估算目标 8000）」百分比条
- **阶段提示**：新增「任务进度：…」阶段行（前端切换为不定进度动画 + 计时）——
  等待豆包响应 / 豆包正在用写作任务撰写正文 / 正文在交付卡片里，正在要求豆包
  直接输出全文 / 等豆包完成上一轮生成后继续。卡片写作任务要跑 2-3 分钟，
  不再让进度条停在 1% 干等
- **精简防御性冗余**（抓故事的核心路径不变，仍稳定 10/10）：
  - 删除从未使用的 selector 候选（_SEND_SELECTORS / _STOP_SELECTORS /
    _RESULT_SELECTORS / _MODEL_SELECTORS）
  - 删除「对话头更多菜单 → 删除对话」DOM 路径（两轮真实探测均无可命中项）；
    删除接口候选 4 → 2，逐条日志降 DEBUG
  - 「文本稳定但仍在生成中」由每轮一条改为只提示一次（同一段链路实测重复 18 行
    → 1 行）；模型检查日志降 DEBUG
  - `--probe` 改为只探测真正在用的 selector + 一次消息状态探测（正文/卡片/生成中）
  - 驱动 941 → 850 行；同一段生成链路的驱动日志 26 行 → 9 行
- 复核：真实 E2E 6261 字、无卡片、格式 **10/10 ✓合规**

### 修复（2026-09-19：网页端丢章节标题，合规稿被误判废稿）
- **根因**：DeepSeek 与豆包一样把 markdown 渲染成 DOM（`## **N**` → `<h2>N</h2>`），
  而 DeepSeek 驱动的 `_read_probe` 直接取容器 innerText → 章节语法整体丢失，
  12 篇稿子无一含 `## **`。格式校验「章节 0 个」必扣 4 分（通道满分只剩 6/10），
  字数略欠（<4000）的完整稿直接判废：真实事故 2026-09-19 上午 3+5+5 共 13 轮，
  8 篇进草稿箱、5 篇废稿里 4 篇是这么丢的（其中一篇 3976 字，差 24 字）
- **主修：把豆包的「逐块重建 markdown」移植到 DeepSeek**——实现抽到
  `web_drivers/base.py` 的 `MARKDOWN_REBUILD_JS`，两个驱动共用同一份
  （h1-h6 → `## **N**`，其余块按段落拼接），避免同类问题再次在两个通道分叉
- **文本侧兜底**：`core/story_text.restore_bare_chapter_headings` 在
  `fix_story_format` 里把「独占一行、从 1 起逐行 +1、至少 3 行」的裸章节号补回
  `## **N**`（判据从严：倒计时 3/2/1、正文里的数字行一律不动）
- **校验侧兼容**：`validate_story_format` 的章节统计改走 `count_chapter_headings`，
  同时认「规范 `## **N**`」「`第N章`」「裸章节号」三种形态，引言判定同步
  （开头就是裸章节号 → 缺引言，一票否决）——API 通道等不做文本修复的调用点同样不再误杀
- **豆包残稿判据**：`wait_complete` 新增「正文末尾停在章节标题 = 没写完」，不再判完成。
  真实事故里那一轮 3 次尝试都只读到「引言 + 第 1 章正文 + 5 个空壳标题」的 597 字
  半截稿，而前三条判据（长度稳定 / 重读一致 / UI 空闲）全部成立；现在等到超时按
  「未完成」交上层重试，不再把半截稿当成品交出去
- **真实数据回归**：2026-09-19 的 5 篇废稿重跑校验，4 篇 DeepSeek 稿由 4/10 变
  8-10/10（合规）、豆包残稿仍 5/10（正确判废）
- 新增合成 DOM 真机探针 `tools/archive/probes/probe_markdown_rebuild.py`
  （不登录不联网，用真实浏览器引擎跑同一段 walker，验证 h2 → `## **N**`）

### 测试
- 新增 tests/test_web_session_lifecycle.py（41 例：会话复用/损坏新开/归属删除/
  reset/驱动框架/API 端点/豆包单元——含正文提取/markdown 重建/卡片兜底/发送确认/
  生成中判定/进度心跳与前端解析同源/阶段提示/写作计划句清洗回归）
- 2026-09-19 补测：story_text 章节三形态容错（裸章节号/第N章/正文数字行不误判/
  裸章节号开头判缺引言）、驱动 walker 同源（deepseek 与 doubao 共用 base 常量 +
  `_read_probe` 注入校验）、豆包残稿判据（真实半截稿 + `wait_complete` 拒绝判完成）
## v4.8.1（2026-09-02）

### 经典模式完整链路（原「单轮」改名）+ 说明文档补全
- 「单轮（完整链路）」正式改名「经典模式 · 完整链路」，并同样支持「发布几篇（轮数）」
  （默认 1，设为 N 即循环完整链路 N 轮，每轮独立选题/生成/审核/发布，轮间小憩 2s，
  看门狗按批量口径放宽总时长）
- 后端轮数语义统一：新增 _RunSpec.rounds（默认 1）专用于经典/纯净完整链路，
  批量模式 publish_count 不受影响
- 说明文档新增「经典模式 vs 纯净模式」五环节对比（选题/提取/生成/审核/发布），
  讲清两条完整链路的约束深度差异与选型建议

### 测试
- 全量 tests/run_all.py：430 例，0 失败 0 错误
## v4.8.0（2026-09-02）

### 新增「纯净模式」（工作台 · 完整链路）

工作台新增纯净模式运行项（运行控制 → 「纯净模式 · 完整链路」）：刻意去掉
选题规则筛选、大模型题材筛选、首答长度门槛、生成格式/字数/章节/去AI味守则
等层层限制，只保留：

- **选题**：有飙升选飙升，无飙升按问题关注量选（不做故事关键词过滤）
- **提取**：首答 ≥「最短回答」（MIN_ANSWER_LENGTH，默认 500 字）+ 点赞 ≥
  「最低点赞」（MATERIAL_MIN_LIKES，门槛逐轮放宽、重试耗尽回退池内最高赞），
  其余长度/体裁/领域不限
- **生成**：给定题目 + 学习参考高赞回答风格，只禁抄袭与洗稿（build_clean_prompt/
  generate_story_clean，无格式硬约束）
- **审核**：core/originality.py 对比新回答与参考高赞回答——本地相似度信号
  （最长公共子串/字符 bigram Dice/句子重复率）+ LLM 综合判定（原创/洗稿/抄袭），
  不通过自动带原因重写（CLEAN_MAX_GEN_ATTEMPTS 次）
- **发布**：与单轮一致，写入知乎草稿箱

配置（config/story.py）：素材点赞门槛即 Web 控制台「设置→选题参数→最低点赞」
（MATERIAL_MIN_LIKES，纯净模式不再另设独立门槛，避免与 UI 设置错位）；
另可调 CLEAN_MAX_GEN_ATTEMPTS / CLEAN_AUDIT_ENABLE（关闭审核则生成后直接发布）。新增 tests/test_originality.py
（19 例：本地信号、审核判定、纯净 prompt、纯净选题/提取、run_clean 契约、
已答过台账、反馈升级）。

首轮实测迭代（2026-09-02 真机一轮：升频选题→已答过→换题→两次洗稿重审→
第三版通过并发布）：
- 「此问题已发布过回答」拒绝时写入 published_topics.jsonl（source=manual），
  跨轮/跨天选题直接跳过，不再每次白耗一轮打开+提取
- 洗稿重试反馈升级为「结构性大改」清单（换背景/人物/事件顺序/台词句式/视角），
  连续两版洗稿再叠加「换完全不同背景」的强指令，收敛速度显著加快
- 审核日志去重：判定依据只由生成循环输出一次
- 生成阶段新增「学习参考回答段落长度」：统计参考回答的段落特征
  （平均/中位/主区间，跳过章节标题与分隔线），注入纯净模式 prompt，
  新回答的段落长短向参考习惯看齐（实测段落偏长的问题）
- 浏览器启动容错（连续运行后偶发「Target page, context or browser has
  been closed」/误报未登录）：启动前与每次重试前自动清理仍占用
  data/browser_profile profile 锁的残留 msedge.exe（只按命令行匹配本
  profile 路径，不碰日常 Edge）；预检失败提示区分「浏览器启动失败」
  与「未登录」，不再只甩一句去登录
- 消除「黑框一闪」：所有后台 PowerShell/taskkill 调用（截图取屏、窗口
  聚焦、残留进程清理）统一走 run_process_silent（CREATE_NO_WINDOW +
  SW_HIDE），运行链路不再弹黑色终端框
- 纯净模式审核新增「段落长度分布对比」（纯数学）：生成后统计生成文的
  段落分布（短<50/中50-150/长>150 占比 + 平均段长），与参考回答对比，
  差异过大（CLEAN_PARAGRAPH_BUCKET_DIFF_MAX=0.55 / AVG_MIN_RATIO=0.30）
  判「段落长度不符」并带原因重写——实测生成故事平均段长 20~80 字波动，
  与参考短段风格对不齐时会被拦截重写
- 纯净模式新增「最短回答」底线：提取的首答需 ≥ 设置里「最短回答」
  （MIN_ANSWER_LENGTH，默认 500 字），一句话/太离谱的题目被过滤掉；
  兜底素材也只从「长度达标」候选中取
- 纯净/经典完整链路支持多轮：运行控制可设「发布几篇（轮数）」，默认 1，
  设置 >1 时把完整链路循环执行多轮（每轮独立选题/生成/审核/发布，轮间
  小憩 2s，看门狗按批量口径放宽总时长）；「单轮（完整链路）」改名
  「经典模式 · 完整链路」，同样可配轮数
- 提取防空转（8/31 实测复现：池内唯一飙升题已答过、其余首答点赞 3~37，
  固定 200 赞门槛 9 连拒整轮报错）：点赞门槛改为逐轮放宽
  （200→120→60，下限 CLEAN_MIN_LIKES_FLOOR=20），重试耗尽仍无合格素材时
  回退取整轮所见最高赞首答（警告后继续，不再报错中断）；选题无飙升/候选
  不足时自动滚动扩池（CLEAN_SELECT_SCREENS=3）

## v4.7.3（2026-09-01）

### 批量质量优先
批量运行改回「单轮链路循环 + 末尾筛选」的原设计（用户实测反馈：旧批量
为效率做的并行化拉低了故事质量；口径改为质量第一、效率第二）。

- BATCH_QUALITY_FIRST=True（默认）：
  - 素材收集 = 逐轮走单轮链路的 extract_content（热度选题 → 并行 5 候选
    取点赞最优 → LLM 问题池筛选），不再整页滚动取前 N 凑数；连续空轮上限
    BATCH_COLLECT_MAX_EMPTY_ROUNDS 防死循环（旧 collect_materials_batch 保留可切回）
  - 生成 = 每篇走 generate_story_with_retry（与单轮一字不差：带失败原因
    反馈的重试循环，最多 STORY_GENERATE_MAX_ATTEMPTS 次，最高分版兜底；
    Web 模式含前端改版自动降级 API）。API 模式保留多线程并行外壳（不伤质量）；
    Web 模式质量优先改为串行（反馈重试需同会话往返，效率让位于质量）
  - 阶段2.5 不再盲重试（生成已重试过），仍不合格者标记废稿不发布
  - 评分择优排序乘账号题材先验 TOPIC_PRIOR_IN_SCORE（发布 top N 向口碑题材倾斜）
- 新增 12 例 tests/test_batch_quality.py（分支锚点守护、单轮式精选去重/异常/空轮、
  单篇质量生成、API 并行/Web 串行、题材先验排序）

### 故事生成逻辑强化（故事质量）
- 引言缺失一票否决：validate_story_format 的「引言存在性」从软扣分改为硬校验——
  正文第一行直接是章节标题 `## **1**` 时，即使其余项满分也判不合格并带原因重试
  （此前只扣 2 分、满分 8/10 照样放行，缺引言废稿因此漏网）；并把「# 第一章/第一章」
  等非标准章节标题也识别为缺引言
- 前 5 段禁虚构人名：行文守则新增硬性条款，开头前 5 段（约引言及 300-500 字）不得
  出现虚构人物全名，用代词/身份/关系/称呼顶着，真名推迟到第 6 段之后
- 学习参考回答的开头引入：生成 prompt 明确要求拆解参考回答「第一句如何抛钩子、
  用什么视角/语气引入人物与事件」，学其开头引入的手法（情节仍严禁搬运）
- 问题题目优先：新增 QUESTION_FIRST_RULE 注入所有生成模式，题目原始要求（题材/人称/
  篇幅/结局/语气）为最高优先级，与本 prompt 写作要求冲突时以题目为准

### 选题质量修复（痛点：通过了硬性筛选却非故事）
- 关键词白名单+反例黑名单：STORY_INCLUDE_KEYWORDS 移除「文推荐/书荒/求文/推文/
  好看的小说/推荐小说/小说推荐/码字/新人写」等推荐/写作教学类词，新增
  STORY_EXCLUDE_KEYWORDS（求推荐/书单/写作教学/变现），_apply_story_filter 改为
  「命中白名单且未命中黑名单」才保留
- _ai_pick_best 不再「退而求其次」：单个候选也过 LLM 筛选；LLM 判定候选全部不适合
  写故事时返回哨兵 _AI_SCREEN_REJECT_ALL 触发整批重新选题，而非回退到点赞最高的非故事题
- _ai_screen_questions 区分「全部被否」与「LLM 失败」：前者中止本批（返回空），后者
  才回退原候选，不再把「全否」误当「不可用」而放行
- 顺带修复 workflows/base.py 3 处 log f-string 格式 bug（logging 抛 TypeError）

### 意见反馈模块（新功能）
- 控制台右上角「反馈」按钮：分类 + 问题描述 + 可选上下文，随时记录使用中遇到的问题
- 存储为 feedback.md（源码态项目根 / 安装态 %APPDATA%\AutoQuill），供后续迭代直接翻阅
- 终端也可用 `python feedback.py "描述" -c 选题`（--list 查看历史）
- 后端 webui/api_feedback.py（POST/GET /api/feedback）+ core/user_feedback.py（线程安全追加）

### 测试
- 新增 tests/test_user_feedback.py 4 例 + test_zhihu_ai_pick.py 重写单候选/全否用例
- 全量 tests/run_all.py：392 例，0 失败 0 错误

## v4.7.2（2026-08-30）

发布数据反馈闭环 + 稳定性保障 + 复盘自动化（P0/P1/P2 一次交付）。

### P0：发布数据反馈闭环（core/feedback_loop.py 从预留桩变为完整实现）
- core/feedback_loop.py 从预留桩变为完整实现：
  - record_story_published(url, title, meta) 与 core/topic_ledger 合流，台账落账追加 version / aid / genre / story_file / session_id——复盘可按版本直接出账，不再靠日志时间戳反推
  - attach_performance(...)：看板抓取时自动把每篇的阅读/赞/评/藏/喜欢写入 data/state/story_performance.jsonl（每条=一篇的一次观测，天然时间序列；幂等不重复）
  - attach_snapshot_rows(...)：兼容新（扁平字段）/老（metrics 字典）两种快照格式
  - seed_from_snapshots()：历史快照一键回填（tools/seed_feedback.py），本次已回填 813 篇、1576 条观测
  - summarize(genre=None)：按题材聚合「发布后日均互动分」（沿用 READER_SCORE_* 权重：赞1/评3/藏2.5/喜欢2，90 天指数衰减），观测不足 2 篇的题材回落全局中位
- 题材反馈加权选题（P0-B）：workflows/zhihu.py _dom_score 在热度分后叠加 topic_genre_multiplier（boost 钳制 0.5~2.0，默认权重 0.5；无数据/未知题材/失败时恒为 1.0，不改变原打分行为）
- 题材分类下沉：GENRE_RULES / genre_of 移入 core/detectors.py（classify_genre），看板 /api/dashboard 语义不变（webui/published.py re-export）
- 配置：config/story.py 新增 FEEDBACK_LOOP_ENABLE / TOPIC_GENRE_PRIOR_ENABLE / TOPIC_GENRE_PRIOR_WEIGHT / TOPIC_GENRE_BOOST_MIN/MAX（默认开启）
- 工具：tools/seed_feedback.py 回填历史快照 + 打印题材先验摘要（幂等，可反复执行）

### P1：生成-守则对齐 + 通道容错 + 提取自适应
- **守则前置**：story_prompt.py 新增 FORMAT_SELF_CHECK_RULE「发布前自检」（引言/章节/量化克制/环境空镜/对话句式/篇幅六条，与 validate_story_format 扣分点一一对应），所有模式生成 prompt 末尾注入——让稿子一次过，减少 8/29 式废稿与重试
- **Web 通道自动降级**：workflows 新增 _generate_web_with_failover——DeepSeek 前端改版/输入框丢失类故障自动降级到 API 通道补跑本轮；同任务连续失败 >= WEB_FAILOVER_MAX_CONSECUTIVE（默认 2）后跳过 Web 直走 API（断路器）；成功重置计数；非界面类异常（超时/风控）照常抛出
- **提取门槛自适应**：workflows/zhihu.py 新增 _adaptive_min_length/_adaptive_min_likes——首轮按 MIN_ANSWER_LENGTH/MATERIAL_MIN_LIKES 原值，之后每轮按 EXTRACT_LENGTH_FACTORS/LIKES_FACTORS 逐级放宽（长度 500→400→300 地板 250；点赞 200→120→60 地板 20），放宽时日志明示，消灭「重试 9 次仍无合格首答」的整轮空转；EXTRACT_ADAPTIVE_RELAX 可整体关闭
- 配置：config/story.py 新增 WEB_FAILOVER_TO_API / WEB_FAILOVER_MAX_CONSECUTIVE / EXTRACT_ADAPTIVE_RELAX / EXTRACT_LENGTH_FACTORS / EXTRACT_LIKES_FACTORS / EXTRACT_MIN_LENGTH_FLOOR / EXTRACT_MIN_LIKES_FLOOR

### P2：复盘自动化
- **tools/version_feedback_report.py**：每周一键复盘工具——日志事件解析（文件/标题/格式分/重试/废稿）→ git 提交时间线归因版本（新→旧自动处理，无版本号的开发提交按提交主题分组）→ 与最新知乎快照按标题归一化匹配（支持截断标题唯一前缀兜底）→ 按版本聚合发布率/格式合规/重试/废稿/互动中位 → 控制台摘要 + --write 落 docs/REVIEW-<日期>.md + 题材先验输出
- **看板「日均赞」列**：webui/published.py 为每行计算 likes_per_day / reads_per_day / engagement_per_day（互动分=赞+3×评+2.5×藏+2×喜欢，与反馈闭环同口径），前端列表新增「日均赞（/天）」列，悬停显示日均互动分——反馈梯度在控制台直接可见
- 工具已用真实数据运行：docs/REVIEW-2026-08-30.md（53 事件 / 356 快照 / 11 版本分组，与手工复盘完全一致）

### 测试
- 新增 tests/test_feedback_loop.py 10 例（落账版本/元数据、幂等观测、双格式回填、题材先验与乘数钳制、稀有题材回落、无数据时选题打分不变）+ tests/test_p1_quality_gates.py 11 例（自检清单注入、Web 降级/断路器/成功重置/非界面异常不吞、提取门槛步进与地板、看板日均指标）+ tests/test_version_report.py 12 例（日志解析、版本归因新→旧、标题归一匹配、截断前缀兜底、歧义不匹配、双格式快照）
- 全量 tests/run_all.py：375 例，0 失败 0 错误（跳过浏览器依赖 2 例）

## v4.7.0（2026-08-26）

本轮聚焦「让故事更有趣」的作者能力升级 + 单次链路可回答性死循环修复。

### 新功能
- 作者蒸馏升级到「法」层：作者技能签名新增 5 个结构性字段——叙述声口 voice_signature、信息控制与反转构造 info_control、爽点/满足机制 satisfaction_mechanics、章末钩子方式 chapter_hooks、读者承诺/题材契约 reader_promises。把"学句长/比喻的味儿"升级为"学节奏、信息差、爽点、声口的法"，生成时注入让模型锁结构而非只仿句式
- 通用写作风格新增 engagement_mechanics（通用「有趣/爽感」机制），默认「通用」模式同样受益
- 生成侧新增「爽感与趣味硬约束」：先压后弹、每节章末钩子、≥2 层套娃真相、一次打脸四部曲、≥2 种爽点类型交替、确立唯一叙述声口、龙头凤尾水蛇腰自检——采样/配方/参考模式全生效
- 旧版作者签名缺失新字段时渲染优雅降级为「（未提炼）」，不阻断生成

### 修复
- 单次完整链路反复回答同一问题的死循环：check_answerable 原先把「编辑回答」（本账号已答过此题）误判为可回答，导致已答问题被反复提取/发布；现按「写回答=未答可答、编辑回答=已答跳过、撤销删除=删过跳过、查看我的回答=发布过跳过」精确区分，已答/已删问题直接跳过并换题（发布通道仍接受写回答/编辑回答，仅选题判定收紧）

### 测试
- 渲染层：旧签名降级与「法」层新字段渲染均验证通过；构建门禁全量测试（unittest）通过

---

## v4.6.1（2026-08-25）

v4.6.0 上线实测后的修复版。

### 修复
- 草稿箱刷新完成后前端进度条不消失：完成/失败时明确清除「正在抓取」状态，成功时弹出「刷新完成」提示（此前成功路径从未被走到，进度条永远停在转圈）
- 安装版快照路径错误：webui/drafts.py 与 published.py 的 `_DATA_DIR` 由 `__file__` 相对路径改为 `core.paths.data("data")`（源码态不变，安装版正确落到 `%APPDATA%\AutoQuill\data`，不再写进安装目录 `_internal`）

### 测试
- 草稿箱/看板/服务端相关 98 例 + 全量 341 例通过；node 语法检查通过

---

## v4.6.0（2026-08-24）

本版为「草稿箱素材模块修复 + Web 模式双头化（评分/筛选走网页版大模型）」修复发布。

### 修复（草稿箱素材模块）
- qid 提取正则语法错误（`/question/(d+)/`，两个斜杠未转义且 `\d` 丢失反斜杠）→ 修正为 `/question\/(\d+)/`，浏览器 evaluate 不再抛 SyntaxError（此前网页端永远显示无草稿）
- 适配知乎草稿卡改版 DOM：标题/时间/正文均为 `div`（时间文本形如「编辑于 5 小时前」，不再有 span/data-tooltip），卡片不再只能抓到 qid
- 字数统计改为抓取服务端草稿全文（`/api/v4/questions/{qid}/draft`），不再显示卡片固定约 200 字摘要；「详情」可查看全文
- 相对时间（分钟/小时/昨天/前天/N 天/周/月前）换算为日期，供排序与筛选
- 草稿列表点击条目可直接在浏览器打开知乎对应编辑页（新标签）；原误删风险消除——删除按真实 qid 精确匹配卡片的「删除」按钮

### 新功能（Web 模式双头化）
- 评分（score_stories）与问题池筛选（screen_question_pool）在 Web 链路下改走 DeepSeek 网页版大模型（与故事生成同一浏览器通道），不再依赖 API Key；Web 不可用时自动回退原顺序/原候选，不阻断链路
- API 链路保持不变：KB Key + 401/403 自动回退生成 Key
- 批量「并行/串行」日志标签按实际通道显示（Web 并行模式不再误标串行）；评分失败提示按模式区分（Web 提示确认网页版登录）

### 测试
- 新增草稿箱（qid 正则、卡片选择器、相对时间、全文转换）与评分 Web 通道（成功/回退）回归测试；全量tests/run_all.py 341 用例通过

---

## v4.5.0（2026-08-23）

本版为「草稿箱 + 去 AI 味 + 自动化测试 + Web 窗口复用 + 大模型问题筛选」综合发布。

### 新增功能
- 草稿箱素材管理模块：预览（题目/摘要/字数/更新时间）、关键词/时间/字数筛选、勾选后批量「从知乎删除」（二次确认，不可逆），不含发布；与看板共用快照层、浏览器互斥与任务状态。
- 去 AI 味体系：
  - 采样参数：frequency/presence penalty 打开（专治复读句式）
  - 行文去 AI 味守则：万能连接词禁词、三连排比/否定排比禁令、中文 AI 高频句式（关联句式/揭露比喻/极值判断/金句）清单
  - 评分新增「语言自然度（去 AI 味）」维度与专项扣分、natural 参考分
  - 本地检测器 tools/ai_flavor_check.py（真人 1/100 vs AI 稿 28/100 区分度）
- Web 网页通道窗口复用：同一会话连续提问（continue_chat，问题连贯、不新开窗口）；并行窗口损坏（长时间无输出/输出错误/重置超限）自动开新窗口补位，一次抖动不重建。
- 大模型问题池筛选：批量与单轮链路在硬性规则（关键词/关注度/点赞门槛）之后，先由 LLM 排除不适合写知乎故事/小说的候选，再挑最适合的 1 个；失败/Web 模式/开关关闭自动回退原规则。开关 config/story.py 的 QUESTION_AI_SCREEN。
- 自动回归测试工具 tools/auto_test.py：一键跑后端 324+ 用例 + Python/app.js 语法 + Playwright 前端全流程回归 + 服务端日志检查，替代人工测试；--quick 供 CI。
- 统一测试入口 tests/run_all.py（本地/CI 共用，自动跳过浏览器依赖用例）+ GitHub Actions CI。

### 可靠性修复
- 批量 watchdog 总时长按模式放宽（batch 60min），避免正常推进被 15 分钟一刀切误杀，日志标注是否用户操作
- 评分 Key 401/403 自动回退「故事生成」Key 重试一次，失败时给出明确提示
- 双击启动黑屏：launcher 顶部 from core.ports 找不到 core 包（pythonw 以 tools/ 为 cwd）→ sys.path 注入 + 打包态兜底
- 草稿/看板快照质量防护：零数据/坏指标不落盘，自动回退最近好快照；双格式归一兼容
- 草稿删除/看板删除单条容错：页面导航异常/找不到按钮自动跳过继续，结束汇总成功/跳过/异常
- 浏览器任务四路互斥：刷新/删除之间禁止并发抢占同一 profile
- 草稿删除完成提示被 loadDrafts 清空 → 保留 8s 完成 toast
- 自动回归工具首发即抓出 4 个问题（含上述黑屏与 toast），全部修复

### 工程化（P0-P3）
- 统一快照层 webui/_snapshot.py（published/drafts 共用）
- server 拆分：browser_tasks / dashboard_api / drafts_api
- 前端抽离 webui/static/style.css + app.js，共享渲染助手去重
- 端口单一来源 core/ports.py；19 个一次性探查脚本归档 tools/archive/probes
- 日志轮转（启动清理 30 天前日志、保留最近 20 份）；关键模块类型注解
- 新增单测：drafts / published 快照 / ai_flavor / 评分回退 / question_screen / 并行 continue / zhihu ai_pick / launcher 等

### 测试
- 全量 tests/run_all.py：336 用例，0 失败 0 错误（跳过的浏览器依赖用例在 CI 自动排除）

---

## v4.4.0（2026-08-23）

与 v4.5.0 同轮迭代（草稿箱主体、去 AI 味主体、批量可靠性、无黑框启动、评分失败提示），详细条目见 v4.5.0；本行保留以对版本提交对齐。

## v4.3.0

修复作者蒸馏/生成链路多问题 + 一键启动（详见 README「版本历史」）。
